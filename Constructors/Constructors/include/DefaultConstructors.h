#ifndef DEFAULTCONSTRUCTORS_H
#define DEFAULTCONSTRUCTORS_H


class DefaultConstructors
{
    public:
        DefaultConstructors();
        DefaultConstructors(int);
        DefaultConstructors(int,int);
        void print();
    private:
        unsigned short x, y;
};

#endif // DEFAULTCONSTRUCTORS_H
